using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MSRBAvatarConfiguration : MonoBehaviour
{
    public float StandardCameraHeight { get { return standardCameraHeight; } }
    [SerializeField] private float standardCameraHeight;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
